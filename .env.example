# ===========================================
# VIBE STACK - Environment Variables Reference
# ===========================================
#
# LOCAL DEV: Run `npx convex dev` - it creates .env.local automatically
#
# SYNC COMMANDS:
#   Pull from Vercel:  pnpm env:pull:vercel
#   Pull from Convex:  pnpm env:pull:convex
#   Push to Convex:    pnpm env:push:convex:development
#   Verify sync:       pnpm env:verify:convex
#
# This file is just a reference - don't copy it.
# ===========================================

# Convex (auto-generated by `npx convex dev`)
CONVEX_DEPLOYMENT=dev:your-project-name
NEXT_PUBLIC_CONVEX_URL=https://your-project.convex.cloud

# Auth
AUTH_SECRET=           # openssl rand -base64 32
SITE_URL=              # https://your-domain.com

# Stripe (optional)
STRIPE_SECRET_KEY=     # https://dashboard.stripe.com/apikeys
STRIPE_WEBHOOK_SECRET= # Stripe webhook signing secret

# CI/CD
CONVEX_DEPLOY_KEY=     # Convex Dashboard → Settings → Deploy Key
