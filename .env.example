# Next.js Environment Variables
# ==============================
#
# LOCAL DEVELOPMENT:
#   Next.js loads .env files in this order (highest priority first):
#   1. .env.development.local - Local overrides (gitignored)
#   2. .env.local             - Local overrides (gitignored)
#   3. .env.development       - Development defaults
#   4. .env                    - Base defaults
#
#   Create your local env:
#     cp .env.example .env.development
#
# VERCEL DEPLOYMENTS:
#   Pull env vars from Vercel with:
#     pnpm env:pull:vercel:development  → .env.development.local
#     pnpm env:pull:vercel:production   → .env.production.local
#
# CONVEX ENV VARS:
#   See .env.convex.example for Convex-specific variables
#   Pull: pnpm env:pull:convex
#   Push: pnpm env:push:convex:development
#
# This file is just a reference - don't copy it.
# ==============================

# Convex (auto-generated by `npx convex dev`)
CONVEX_DEPLOYMENT=dev:your-project-name
NEXT_PUBLIC_CONVEX_URL=https://your-project.convex.cloud

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Auth (if using Convex Auth)
# AUTH_SECRET=           # openssl rand -base64 32
# SITE_URL=              # https://your-domain.com
