# Convex CLI Deploy Keys
# Get from: https://dashboard.convex.dev → Project Settings → Deploy Keys
#
# Create separate files for each environment:
#   .env.convex-cli.development  (optional - dev uses local auth by default)
#   .env.convex-cli.production
#   .env.convex-cli.preview
#
# The Convex CLI uses these to authenticate when running:
#   npx convex env list --env-file .env.convex-cli.production
#   npx convex deploy --env-file .env.convex-cli.production

CONVEX_DEPLOY_KEY=prod:your-project|your-deploy-key-here
