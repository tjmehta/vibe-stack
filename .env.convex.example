# Convex Environment Variables
# =============================
# These variables are stored in Convex deployments (not in your repo).
# This file documents what vars your Convex backend expects.
#
# MANAGING CONVEX ENV VARS:
#   Pull from Convex:  pnpm env:pull:convex
#   Push to Convex:    pnpm env:push:convex:development
#   Verify sync:       pnpm env:verify:convex
#
# FILES CREATED BY PULL:
#   .env.convex.development - Dev deployment vars (local reference)
#   .env.convex.production  - Prod deployment vars (local reference)
#   .env.convex.preview     - Preview deployment vars (local reference)
#
# CLI COMMANDS (manual):
#   Development: npx convex env set KEY "value"
#   Production:  npx convex env set KEY "value" --env-file .env.convex-cli.production
#   Preview:     npx convex env set KEY "value" --env-file .env.convex-cli.preview --preview-name <branch>
#
# DASHBOARD:
#   Project Defaults: Project Settings → Environment Variables (for new previews)
#   Per deployment: Deployment → Settings → Environment Variables

# =============================================================================
# EXAMPLE VARIABLES (add your project-specific vars below)
# =============================================================================

# App URL (for generating links in emails, redirects, etc.)
# SITE_URL=https://your-app.com

# =============================================================================
# AUTH (if using Convex Auth)
# =============================================================================
# AUTH_SECRET=xxx  # openssl rand -base64 32

# =============================================================================
# AUTH PROVIDERS (if using OAuth)
# =============================================================================
# GOOGLE_CLIENT_ID=xxx
# GOOGLE_CLIENT_SECRET=xxx
# GITHUB_CLIENT_ID=xxx
# GITHUB_CLIENT_SECRET=xxx

# =============================================================================
# STRIPE (if using payments)
# =============================================================================
# STRIPE_SECRET_KEY=sk_xxx
# STRIPE_WEBHOOK_SECRET=whsec_xxx
